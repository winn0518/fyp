# Core Flask - keep these
Flask==2.3.3
flask-cors==4.0.0

# Production server
gunicorn==20.1.0

# Basic data processing (skip heavy ones initially)
pandas==1.5.3  # Use older, lighter version
numpy==1.24.3

# LangChain - minimal version
langchain==0.0.350  # Use specific older version that's stable

# OpenAI (if using)
openai==0.28.1  # Older stable version

# ChromaDB - minimal
chromadb==0.4.18  # Older version

# Required by ChromaDB
pypdf==3.17.0

# Environment
python-dotenv==1.0.0

# Remove these heavy dependencies for now:
# matplotlib, seaborn, scikit-learn, scipy, torch, transformers, sentence-transformers