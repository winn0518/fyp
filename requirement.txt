# Core Flask
Flask==2.3.0
flask-cors==3.0.10
gunicorn==20.1.0

# Basic utilities
python-dotenv==0.19.2

# Minimal LangChain - avoid complex deps for now
langchain==0.0.200

# OpenAI
openai==0.28.0

# Skip heavy dependencies initially
# pandas, numpy, matplotlib, etc.